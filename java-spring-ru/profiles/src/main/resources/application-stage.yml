# BEGIN
spring:
  datasource:
    url: jdbc:h2:file:./test
    username: admin
    password: admin
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true  # Включает H2-консоль для отладки
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# END
